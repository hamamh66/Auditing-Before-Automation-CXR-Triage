# Python 3.10+ recommended.
# timm, ImageHash and seaborn carry the pins declared in the notebook's
# own REQUIRED block; the remaining entries are lower bounds for the
# versions the workflow was developed against.

numpy>=1.26
pandas>=2.2
scipy>=1.11
scikit-learn>=1.5
joblib>=1.3
matplotlib>=3.8
seaborn>=0.13.2
pillow>=10.0

torch>=2.3
torchvision>=0.18
timm>=1.0.19

ImageHash>=4.3.2
kagglehub>=0.3.12
